# 🔍 Отладка Support Agent - Инструкция

## ❓ Почему появлялись запросы безопасности?

Клиент имеет встроенную **защиту от опасных команд**:
- `os_run` - запуск программ
- `os_delete` - удаление файлов
- `reg_write` - запись в реестр
- `os_kill` - завершение процессов

По умолчанию клиент **запрашивает подтверждение** перед выполнением этих действий.

## ✅ Решение: Флаг `--auto-approve`

Для автоматического тестирования используй флаг `--auto-approve`:

```bash
SupportAgent.exe --server "https://xelth.com/AGI" --task "Твоя задача" --auto-approve
```

## 🚀 Быстрый запуск для тестирования

### Вариант 1: Двойной клик (самый простой)
1. Найди файл: `RUN_DEBUG.bat`
2. Дважды кликни на него
3. Окно консоли останется открытым
4. Лог сохранится в `debug_output.txt`

### Вариант 2: PowerShell
```powershell
cd C:\Users\Dmytro\xelthagi\client\SupportAgent
.\run_visible_debug.ps1
```

### Вариант 3: Полный CI/CD цикл (Build + Run)
```batch
cd C:\Users\Dmytro\xelthagi\client\SupportAgent
ci_cycle.bat
```

## 📝 Что изменилось

✅ **run_visible_debug.ps1** - теперь использует `--auto-approve`
✅ **ci_cycle.bat** - теперь использует `--auto-approve`
✅ **RUN_DEBUG.bat** - новый файл для быстрого запуска

## 🎯 Тест калькулятора

Скрипт `run_visible_debug.ps1` запускает простой тест:
- **Задача**: "Open Calculator and type 123"
- **Сервер**: https://xelth.com/AGI
- **Режим**: Auto-approve включён

## 📊 Лог файлы

Все выполнения сохраняются в:
- `../../debug_output.txt` - последний запуск через run_visible_debug.ps1

## 💡 Совет

Если видишь странные символы в консоли (╔═══╗), это нормально - это просто box-drawing characters.
Можно игнорировать или переключить кодировку консоли на UTF-8.
